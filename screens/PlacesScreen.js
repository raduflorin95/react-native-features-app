import React from "react";
import { View, Text, StyleSheet } from "react-native";

const PlacesScreen = (props) => {
  return (
    <View>
      <Text>Places Listing Screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default PlacesScreen;
