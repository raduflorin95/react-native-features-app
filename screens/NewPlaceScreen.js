import React from "react";
import { View, Text, StyleSheet } from "react-native";

const NewPlaceScreen = (props) => {
  return (
    <View>
      <Text>This is the New Places Screen!</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default NewPlaceScreen;
